const { sequelize } = require("../bd");
const Sequelize = require('sequelize');
      
const  = sequelize.define('', {
undefined: {  type: Sequelize.undefined, allowNull: undefined, unique: false }}, 
{tableName: ""});

module.exports ={ 
  ,
}
